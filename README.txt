SPOOKYVERSE HOMEPAGE UPDATE

These are replacement files for the existing SpookyVerse project:
1. index.html -> replace the current root index.html
2. css/style.css -> replace the current css/style.css

Keep all existing images, other HTML pages, and js/script.js.

This update is intentionally limited to the homepage and shared stylesheet so the rest of the project is not destroyed.
